import ScrollScrubberClient from "@/components/ScrollScrubberClient"

export default function Home() {
  return (
    <main className="bg-black min-h-screen">
      <ScrollScrubberClient />
    </main>
  )
}
